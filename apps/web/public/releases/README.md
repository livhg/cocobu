# Releases Directory

請將編譯完成的 Android APK 安裝檔放置在此資料夾：
- `cocobu-latest.apk` (主要下載端點)
- `cocobu-v1.0.0.apk` (版本封存)

放在此資料夾後，訪客將可透過 `https://cocobu.online/releases/cocobu-latest.apk` 直接下載。
若您使用 GitHub Releases 託管 APK，也可直接修改 `/api/version` 與 `public/version.json` 中的 `apkUrl` 為 GitHub Release 連結。
