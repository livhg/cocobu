[phases.setup]
nixPkgs = ["nodejs_18", "npm"]

[phases.install]
cmds = [
  "npm ci --include=dev"
]

[phases.build]
cmds = [
  "npm run build:api"
]

[start]
cmd = "npm run db:migrate:deploy && npm run start --workspace=@cocobu/api"
